describe('Data', () => {
  // Data refers to topics like state, store, redux, vuex, etc.
  // These are some basic things you need to know about managing data
  it('Create a basic data object that stores a piece of state', () => {
    // FIXME: create an object with a property of 'myDataProp'
    
    expect(state).toHaveProperty('myDataProp')
  })
})